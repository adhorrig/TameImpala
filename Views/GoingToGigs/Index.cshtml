@model IEnumerable<TameImpala.Models.GoingToGig>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UserID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UserID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GoingToGigID }) |
            @Html.ActionLink("Details", "Details", new { id=item.GoingToGigID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GoingToGigID })
        </td>
    </tr>
}

</table>
